# Preserve the upstream lockfile's peer-resolution mode without --force.
# vite-plugin-restart@0.4.1 declares Vite <=5; the approved app uses Vite 7
# and does not enable that plugin. Keep dependency versions frozen for v1.0.0.
legacy-peer-deps=true
